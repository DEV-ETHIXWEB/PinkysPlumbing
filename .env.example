# This is a fully static Astro site with no server runtime and no
# build-time environment variable usage today - `grep -rn "import.meta.env"`
# across src/ currently returns nothing.
#
# Nothing is required to build or run this project locally. When the
# contact form is wired to a real backend (see the comment in
# src/components/islands/ContactForm.tsx) or analytics/tracking IDs are
# added, list the required variables here with placeholder values - never
# real secrets.
